## 1.序列和索引
### 序列
- **序列**是一个用于存储连续多个值的连续空间，每个值都对应一个整数的编号，称为**索引**
- 序列类型包括字符串、列表、元组、集合和字典
### 索引
- 正向**递增**索引 从左到右 从0开始
- 反向**递减**索引 从右到左 从-1开始
```python
s = 'helloworld'
for i in range(0,len(s)):
	print(i,s[i],end='\t\t')
```